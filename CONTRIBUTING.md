# Contributing

1. Fork the repository and create a feature branch.
2. Keep changes focused and include tests where practical.
3. Run `python -m py_compile app/main.py` before submitting.
4. Describe operational and restore implications in the pull request.

Bug reports should include the ContBak version, Docker Engine version, platform, relevant logs, and whether the affected mount is a named volume or bind mount.
